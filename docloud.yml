version: 2.0

build:
  lite_image:
    compile:
      dockerfile_path: Dockerfile.compile
      build_dir: /
      cache: false

    extract:
      - /go/bin/app

    package:
        dockerfile_path: Dockerfile.package
        build_dir: /
        cache: false
